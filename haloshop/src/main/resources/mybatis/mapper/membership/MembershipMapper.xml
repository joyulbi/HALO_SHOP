<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.haloshop.membership.MembershipMapper">

    <!-- 등급 등록 -->
    <insert id="insert" parameterType="Membership">
        INSERT INTO membership (name, price, price_point)
        VALUES (#{name}, #{price}, #{pricePoint})
    </insert>

    <!-- 등급 전체 조회 -->
    <select id="findAll" resultType="Membership">
        SELECT * FROM membership ORDER BY id ASC
    </select>

    <!-- 등급 상세 조회 -->
    <select id="findById" parameterType="int" resultType="Membership">
        SELECT * FROM membership WHERE id = #{id}
    </select>

    <!-- 등급 수정 -->
    <update id="update" parameterType="Membership">
        UPDATE membership
        SET name = #{name},
            price = #{price},
            price_point = #{pricePoint}
        WHERE id = #{id}
    </update>

    <!-- 등급 삭제 -->
    <delete id="delete" parameterType="int">
        DELETE FROM membership WHERE id = #{id}
    </delete>

</mapper>
