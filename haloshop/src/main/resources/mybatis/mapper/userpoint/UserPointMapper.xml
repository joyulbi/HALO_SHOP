<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.haloshop.userpoint.UserPointMapper">

    <resultMap id="UserPointMap" type="com.company.haloshop.dto.shop.UserPointDto">
        <id property="id" column="id"/>
        <result property="accountId" column="account_id"/>
        <result property="totalPayment" column="total_payment"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="UserPointMap">
        SELECT * FROM user_point
    </select>

    <select id="findById" parameterType="long" resultMap="UserPointMap">
        SELECT * FROM user_point WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.company.haloshop.dto.shop.UserPointDto">
        INSERT INTO user_point (account_id, total_payment, updated_at)
        VALUES (#{accountId}, #{totalPayment}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.company.haloshop.dto.shop.UserPointDto">
        UPDATE user_point
        SET total_payment = #{totalPayment},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM user_point WHERE id = #{id}
    </delete>

</mapper>
